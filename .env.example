# Kniffel Umgebungsvariablen
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an

# ===========================================
# PRODUKTIONSUMGEBUNG KONFIGURATION
# ===========================================

# Datenbank Konfiguration
POSTGRES_DB=kniffel
POSTGRES_USER=kniffel_user
POSTGRES_PASSWORD=IHR_SICHERES_PASSWORT_HIER

# JWT Secret für Authentifizierung (generieren Sie einen sicheren Schlüssel)
# Generieren mit: openssl rand -base64 32
JWT_SECRET=IHR_SUPER_SICHERER_JWT_SCHLUESSEL_HIER

# ===========================================
# DEPLOYMENT OPTION A: Eine Domain (Empfohlen)
# ===========================================
# Frontend URL (für CORS und Socket.IO)
FRONTEND_URL=https://kniffel.ladisch-energie.de

# ===========================================
# DEPLOYMENT OPTION B: Backend-Subdomain
# ===========================================
# Uncomment für separate Backend-Subdomain:
# FRONTEND_URL=https://kniffel.ladisch-energie.de
# BACKEND_URL=https://api.ladisch-energie.de

# ===========================================
# ENTWICKLUNGSUMGEBUNG
# ===========================================
# Für lokale Entwicklung verwenden Sie:
# POSTGRES_PASSWORD=kniffel_password
# JWT_SECRET=development-jwt-secret-change-in-production
# FRONTEND_URL=http://localhost:3000
# BACKEND_URL=http://localhost:3001

# ===========================================
# SICHERHEITSHINWEISE
# ===========================================
# 1. Ändern Sie ALLE Passwörter und Secrets für die Produktion
# 2. Verwenden Sie starke, einzigartige Passwörter
# 3. Generieren Sie sichere JWT-Secrets
# 4. Setzen Sie niemals Entwicklungs-Credentials in der Produktion ein
