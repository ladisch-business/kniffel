# Backend Umgebungsvariablen
# Kopieren Sie diese Datei zu backend/.env und passen Sie die Werte an

# ===========================================
# PRODUKTIONSUMGEBUNG KONFIGURATION
# ===========================================

NODE_ENV=production
PORT=3001

# Datenbank URL (für Docker: postgres:5432, für lokal: localhost:5432)
DATABASE_URL=postgresql://kniffel_user:IHR_SICHERES_PASSWORT_HIER@postgres:5432/kniffel

# JWT Secret für Authentifizierung (muss mit Haupt-.env übereinstimmen)
# Generieren mit: openssl rand -base64 32
JWT_SECRET=IHR_SUPER_SICHERER_JWT_SCHLUESSEL_HIER

# ===========================================
# DEPLOYMENT OPTION A: Eine Domain (Empfohlen)
# ===========================================
# Frontend URL für CORS und Socket.IO
FRONTEND_URL=https://kniffel.ladisch-energie.de
CORS_ORIGIN=https://kniffel.ladisch-energie.de

# ===========================================
# DEPLOYMENT OPTION B: Backend-Subdomain
# ===========================================
# Für separate Backend-Subdomain verwenden Sie:
# FRONTEND_URL=https://kniffel.ladisch-energie.de
# CORS_ORIGIN=https://kniffel.ladisch-energie.de

# ===========================================
# ENTWICKLUNGSUMGEBUNG
# ===========================================
# Für lokale Entwicklung verwenden Sie:
# NODE_ENV=development
# DATABASE_URL=postgresql://kniffel_user:kniffel_password@localhost:5432/kniffel
# JWT_SECRET=development-jwt-secret-change-in-production
# FRONTEND_URL=http://localhost:3000
# CORS_ORIGIN=http://localhost:3000

# ===========================================
# ZUSÄTZLICHE KONFIGURATION
# ===========================================
# Session Secret (optional, für erweiterte Session-Verwaltung)
# SESSION_SECRET=IHR_SESSION_SECRET_HIER

# Log Level (optional)
# LOG_LEVEL=info

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
